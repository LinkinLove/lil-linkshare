<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login with Discord</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #36393f;
            color: #fff;
            font-family: 'Roboto', sans-serif;
        }

        .login-container {
            text-align: center;
            background-color: #2f3136;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 2px 10px 0 rgba(0,0,0,.2);
            width: 480px;
            transition: all 0.3s ease;
        }

        .login-container:hover {
            box-shadow: 0 8px 25px 0 rgba(0,0,0,.3);
            transform: translateY(-5px);
        }

        h1 {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 30px;
            color: #ffffff;
        }

        .discord-logo {
            width: 120px;
            margin-bottom: 20px;
        }

        .login-btn {
            display: inline-block;
            margin-top: 20px;
            padding: 12px 24px;
            font-size: 16px;
            font-weight: 500;
            background-color: #7289da;
            color: #fff;
            text-decoration: none;
            border-radius: 3px;
            transition: background-color 0.2s ease;
        }

        .login-btn:hover {
            background-color: #677bc4;
        }

        .login-btn:active {
            transform: translateY(1px);
        }

        .description {
            margin-top: 20px;
            font-size: 14px;
            color: #b9bbbe;
            line-height: 1.4;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .login-container {
            animation: fadeIn 0.6s ease-out;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <img src="https://assets-global.website-files.com/6257adef93867e50d84d30e2/636e0a69f118df70ad7828d4_icon_clyde_blurple_RGB.svg" alt="Discord Logo" class="discord-logo">
        <h1>Login to Your Server</h1>
        <p class="description">Connect with your community and join the conversation. Your server awaits!</p>
        <a class="login-btn" href="/auth/discord?suffix=<%= suffix %>">Login with Discord</a>
    </div>
</body>
</html>
